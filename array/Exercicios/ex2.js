function converterAnosParaDias(num){
    const idade = num
    
    for(let i = 0; i <= idade; i++)  {
        var dias = 365
        var tDias = dias * idade
    }
    return console.log(tDias)
}

converterAnosParaDias(25)